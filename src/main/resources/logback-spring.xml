<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd'T'HH:mm:ss.SSSX} %-5level [%thread] traceId=%X{traceId:-NA} %logger{36} - %msg%n"/>

    <!-- Console appender legível -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Config básica: root em INFO -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>

    <!-- Exemplo de logger mais verboso -->
    <logger name="org.springframework.web" level="INFO"/>

    <!-- Para ativar formato JSON minimalista (opcional) instale logstash-logback-encoder e descomente bloco abaixo -->
    <!--
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <timeZone>UTC</timeZone>
                </timestamp>
                <pattern>
                    <pattern>{"level":"%level","logger":"%logger{36}","traceId":"%X{traceId}","message":"%msg"}</pattern>
                </pattern>
            </providers>
        </encoder>
    </appender>
    -->
</configuration>
